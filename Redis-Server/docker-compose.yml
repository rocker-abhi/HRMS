version: '3.8'

services:
  redis:
    build: .
    container_name: hrms-redis
    ports:
      - "6379:6379"
    volumes:
      - hrms_redis_data:/data
    restart: always

volumes:
  hrms_redis_data:
    driver: local
